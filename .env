# Docker Compose Environment Variables
# These variables are used for variable substitution in docker-compose files
# This file is read automatically by Docker Compose

# Image tags
TAG=dev

# MongoDB connection details for mongodb-exporter
# These must match the values in .env.dev and seed service defaults
MONGO_DB_HOSTNAME=mongodb
MONGO_DB_PORT=27017
MONGO_DB_DATABASE=nestjs_local
MONGO_DB_METRICS_USERNAME=monitoring@system.com
MONGO_DB_METRICS_PASSWORD=MonitoringSystem123!
